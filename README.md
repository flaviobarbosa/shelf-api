# shelf-api
Personal library management API built with Node.js, TypeScript and Clean Architecture. Register books via ISBN lookup (Open Library + Google Books fallback), track reading status and manage loans.
